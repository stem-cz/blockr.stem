% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_chart_block.R
\name{new_stem_chart_block}
\alias{new_stem_chart_block}
\title{STEM Chart block}
\usage{
new_stem_chart_block(
  var = character(),
  type = c("barplot", "inline"),
  weight = character(),
  label_size = 14,
  palette = "div1",
  direction = 1,
  labels = TRUE,
  label_accuracy = 1,
  ...
)
}
\arguments{
\item{var}{Name of the variable (column) to plot. When empty, the first
column of the upstream data is used.}

\item{type}{Chart type, one of \code{"barplot"} or \code{"inline"}.}

\item{weight}{Optional name of a numeric column of survey weights to pass to
the stemtools function. Empty (the default) plots unweighted counts.}

\item{label_size}{Size in points of the inner data labels (the percentage
labels drawn on the bars). Defaults to \code{14}; use \code{NA} to keep the stemtools
default sizing instead. To restyle the axis/legend text, use
\code{\link[=new_theme_stem_block]{new_theme_stem_block()}} downstream instead.}

\item{palette}{Name of a Stem palette passed to the plotting function, e.g.
\code{"div1"}. See \code{\link[stemtools:stem_palette]{stemtools::stem_palette()}}.}

\item{direction}{Palette direction: \code{1} (default) or \code{-1} to reverse.}

\item{labels}{If \code{TRUE}, print a percentage label on each bar.}

\item{label_accuracy}{Rounding accuracy of labels; \code{1} gives whole numbers,
\code{0.1} one decimal place.}

\item{...}{Forwarded to \code{\link[blockr.ggplot:new_ggplot_transform_block]{blockr.ggplot::new_ggplot_transform_block()}}.}
}
\value{
A ggplot transform block object of class \code{stem_chart_block}.
}
\description{
A blockr transform block that plots a single variable from an upstream data
frame using one of the \pkg{stemtools} plotting functions. The user picks a
variable and chooses between a bar plot (\code{\link[stemtools:stem_barplot]{stemtools::stem_barplot()}}) and an
inline plot (\code{\link[stemtools:stem_inline]{stemtools::stem_inline()}}). Styling arguments (label size,
palette, label options) are tucked under an "Advanced options" disclosure.
}
\examples{
new_stem_chart_block(var = "Species", type = "barplot", label_size = 5)

}
