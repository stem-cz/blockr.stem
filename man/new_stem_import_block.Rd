% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_import_block.R
\name{new_stem_import_block}
\alias{new_stem_import_block}
\alias{stem_import_volumes}
\title{STEM Import block}
\usage{
new_stem_import_block(
  file_path = character(),
  args = list(),
  volumes = stem_import_volumes(),
  ...
)

stem_import_volumes()
}
\arguments{
\item{file_path}{Initial file path. Normally left empty and set by picking a
file in the browser.}

\item{args}{Named list of format-specific reader options (see Details).
Normally left empty and set via the gear popover.}

\item{volumes}{Named character vector of root directories the file browser may
navigate, passed to \code{\link[shinyFiles:shinyFileChoose]{shinyFiles::shinyFileChoose()}}. Defaults to
\code{\link[=stem_import_volumes]{stem_import_volumes()}} (Home and the filesystem root).}

\item{...}{Forwarded to \code{\link[blockr.core:new_block]{blockr.core::new_block()}}.}
}
\value{
A data block object of class \code{stem_import_block}.
}
\description{
A blockr data block that imports a data set from a file - like the
"Import Data" block (\code{read_block}) from \strong{blockr.io}, but the file is chosen
with a graphical point-and-click file browser (via \pkg{shinyFiles}) instead
of by typing a path. It is a root node in an analysis graph: it takes no data
input and provides data to down-stream blocks.

\code{stem_import_volumes()} returns the default set of root directories offered by
the file browser: the user's home directory and the filesystem root.
}
\details{
The selected file is read with the reader that matches its format, mirroring
\code{read_block}: delimited text (\code{.csv}/\code{.tsv}/\code{.txt}/...) with \code{\link[readr:read_csv]{readr::read_csv()}}
and friends, spreadsheets (\code{.xlsx}/\code{.xls}/...) with \code{\link[readxl:read_excel]{readxl::read_excel()}}, and
everything else (\code{.rds}, \code{.dta}, \code{.parquet}, ...) with \code{\link[rio:import]{rio::import()}}. SPSS
files (\code{.sav}/\code{.zsav}/\code{.por}) are read with \code{\link[haven:read_spss]{haven::read_spss()}} and then
passed through \code{\link[haven:as_factor]{haven::as_factor()}}, so labelled numeric columns arrive as
proper factors rather than numeric codes.
A gear popover next to the file button exposes the format-specific options
(CSV delimiter/quote/encoding/skip/rows/header; Excel sheet/range/skip/rows/
header), shown only for the matching file type - just like \code{read_block}.

This exists because \code{blockr.core}'s \code{filebrowser_block} builds its output
expression as the bare file path (\code{bquote(.(file), ...)}) rather than a call
that reads the file, so picking a \code{.rds} file yields the path string instead
of the data. This block wraps the picked path in a real read call.
}
\examples{
new_stem_import_block()

}
