% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_var_selector_block.R
\name{new_stem_var_selector_block}
\alias{new_stem_var_selector_block}
\alias{block_output.stem_var_selector_block}
\title{STEM Variable Selector block}
\usage{
new_stem_var_selector_block(
  var = character(),
  weight = character(),
  group = character(),
  ...
)

\method{block_output}{stem_var_selector_block}(x, result, session)
}
\arguments{
\item{var}{Name of the variable to select. When empty, the first categorical
variable is used. Normally set by clicking a table row.}

\item{weight}{Optional name of a numeric column of survey weights to carry
forward. Empty (the default) means unweighted. When set, the block outputs
the weight column alongside the selected variable and tags it so a
downstream \code{\link[=new_stem_chart_block]{new_stem_chart_block()}} / \code{\link[=new_stem_visualize_block]{new_stem_visualize_block()}} weights
the plot automatically.}

\item{group}{Optional name of a categorical column to break the plot down by.
Empty (the default) means no grouping. When set, a downstream bar plot draws
one stacked bar per category of this grouping variable (on the axis) and
shows the \emph{selected} variable as the coloured fill series - so the number of
colours is the selected variable's category count. Grouping is only applied
to \code{stem_barplot()}, not \code{stem_inline()}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}

\item{x, result, session}{Passed by blockr when rendering the block output.}
}
\value{
A transform block object of class \code{stem_var_selector_block}.
}
\description{
A blockr transform block that lists the categorical (factor / character)
variables of an upstream data frame in a searchable table showing each
variable's name, its label (\code{attr(x, "label")}) and its number of categories.
Clicking a row selects that variable; the block outputs the single selected
column (its label preserved), ready to feed a \code{\link[=new_stem_chart_block]{new_stem_chart_block()}} for
plotting - and a \code{\link[=new_theme_stem_block]{new_theme_stem_block()}} after that for styling.

The block's output panel shows the selected variable's (weighted) frequency
distribution rather than the raw responses. The block's \emph{value} passed to
downstream blocks is still the underlying data, so a STEM Chart can plot it.
}
\details{
The table and the plot live in separate blocks that can be moved around
independently, so the chart's advanced settings and a downstream theme block
stay fully accessible. Compose it as Selector -> \code{\link[=new_stem_chart_block]{new_stem_chart_block()}} ->
\code{\link[=new_theme_stem_block]{new_theme_stem_block()}}.
}
\examples{
new_stem_var_selector_block()

}
