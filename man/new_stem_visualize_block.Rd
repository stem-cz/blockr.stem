% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_visualize_block.R
\name{new_stem_visualize_block}
\alias{new_stem_visualize_block}
\title{STEM Visualize block}
\usage{
new_stem_visualize_block(
  var = character(),
  type = c("barplot", "inline"),
  weight = character(),
  label_size = 14,
  palette = "div1",
  direction = 1,
  labels = TRUE,
  label_accuracy = 1,
  ink = "black",
  paper = "white",
  accent = "#35978F",
  family = "Calibri",
  font_size = NA_real_,
  padding = stem_default_padding,
  ...
)
}
\arguments{
\item{var}{Name of the variable (column) to plot. When empty, the first
column of the upstream data is used.}

\item{type}{Chart type, one of \code{"barplot"} or \code{"inline"}.}

\item{weight}{Optional name of a numeric column of survey weights. Empty (the
default) plots unweighted counts.}

\item{label_size}{Size in points of the inner data labels. Defaults to \code{14};
\code{NA} keeps the stemtools default.}

\item{palette, direction, labels, label_accuracy}{Chart styling passed to the
stemtools function (see \code{\link[=new_stem_chart_block]{new_stem_chart_block()}}).}

\item{ink, paper, accent, family}{Stem theme arguments (see
\code{\link[=new_theme_stem_block]{new_theme_stem_block()}} and \code{\link[stemtools:theme_stem]{stemtools::theme_stem()}}).}

\item{font_size}{Base text size in points for the axis/legend/title text.
\code{NA} (the default) keeps the theme's own sizing.}

\item{padding}{Padding in millimetres added to the plot margin so the extreme
axis labels (e.g. "0 \%" / "100 \%") are not clipped. Defaults to 8 mm.}

\item{...}{Forwarded to \code{\link[blockr.ggplot:new_ggplot_transform_block]{blockr.ggplot::new_ggplot_transform_block()}}.}
}
\value{
A ggplot transform block object of class \code{stem_visualize_block}.
}
\description{
A single blockr block that plots a variable with a \pkg{stemtools} function
\emph{and} applies the Stem theme, so the final, styled chart is produced in one
place - combining everything \code{\link[=new_stem_chart_block]{new_stem_chart_block()}} and
\code{\link[=new_theme_stem_block]{new_theme_stem_block()}} offer without having to wire (or jump between) two
blocks. Chart options and theme options live under separate "Advanced"
disclosures.
}
\examples{
new_stem_visualize_block(var = "Species", accent = "#35978F")

}
