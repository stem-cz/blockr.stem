% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pptx-chart.R
\name{stem_write_pptx_chart}
\alias{stem_write_pptx_chart}
\title{Export a STEM plot as a native, editable PowerPoint chart}
\usage{
stem_write_pptx_chart(plot, file, width = NA_real_, height = NA_real_)
}
\arguments{
\item{plot}{A ggplot object produced by a STEM chart / visualize block.}

\item{file}{Path of the \code{.pptx} file to write.}

\item{width, height}{Size of the chart on the slide, in centimetres. Default
\code{NA} fills the slide (leaving a small margin); pass explicit values to place
a fixed-size, centred chart instead.}
}
\value{
\code{file}, invisibly.
}
\description{
Writes a \code{.pptx} file containing a \strong{native Microsoft Office chart} - a real
\code{c:chart} object backed by an embedded Excel worksheet - rather than a picture
of the plot. The recipient can open the deck and use \emph{Chart Design > Edit Data}
to change the numbers by hand in Excel, restyle the chart, etc.
}
\details{
Because a native Office chart is built from \emph{data} (not pixels), this only
works for the STEM bar / inline plots, whose summarised data and aesthetic
mapping are recovered from the ggplot object (\code{\link[stemtools:stem_barplot]{stemtools::stem_barplot()}},
\code{\link[stemtools:stem_inline]{stemtools::stem_inline()}}). The category axis, the (optional) coloured series
and the values are read back from the plot; the Stem palette colours are
carried over to the chart's series so it looks right out of the box. Any other
ggplot raises an error - export those as PNG or SVG instead.

The chart inherits the plot's Stem theme (font family and ink colour from
\code{\link[stemtools:theme_stem]{stemtools::theme_stem()}}, legend on top) so it matches the on-screen plot.
Being a native chart it can be freely resized in PowerPoint, so it is placed
filling the slide by default rather than at a fixed centimetre size.

Requires the \pkg{mschart} and \pkg{officer} packages.
}
