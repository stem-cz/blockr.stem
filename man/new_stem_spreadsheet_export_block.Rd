% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_spreadsheet_export_block.R
\name{new_stem_spreadsheet_export_block}
\alias{new_stem_spreadsheet_export_block}
\alias{block_output.stem_spreadsheet_export_block}
\title{STEM Excel Export block}
\usage{
new_stem_spreadsheet_export_block(
  include_char = FALSE,
  exclude = character(),
  group = character(),
  weight = character(),
  percent = TRUE,
  na_rm = TRUE,
  ...
)

\method{block_output}{stem_spreadsheet_export_block}(x, result, session)
}
\arguments{
\item{include_char}{When \code{TRUE}, character columns are tabulated alongside
factor columns; when \code{FALSE} (the default) only factors are, matching
\code{\link[spreadview:get_categorical_vars]{spreadview::get_categorical_vars()}}'s default.}

\item{exclude}{Character vector of variable names to drop from the automatic
categorical selection (passed to \code{get_categorical_vars()}'s \code{exclude}).
Empty (the default) excludes nothing.}

\item{group}{Optional character vector of categorical variables to break the
frequency tables down by (passed to \code{compose_spreadsheet()}'s \code{group}).
Empty (the default) means no grouping.}

\item{weight}{Optional name of a numeric column of survey weights (passed to
\code{compose_spreadsheet()}'s \code{weight}). Empty (the default) means unweighted.}

\item{percent}{When \code{TRUE} (the default) cells are formatted as percentages,
otherwise as plain counts (\code{compose_spreadsheet()}'s \code{percent}).}

\item{na_rm}{When \code{TRUE} (the default) missing values are dropped from the
tables (\code{compose_spreadsheet()}'s \code{na.rm}).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}

\item{x, result, session}{Passed by blockr when rendering the block output.}
}
\value{
A transform block object of class \code{stem_spreadsheet_export_block}.
}
\description{
A blockr block that exports the upstream data set as a \strong{readable MS Excel
spreadsheet} of frequency tables, built with
\code{\link[spreadview:compose_spreadsheet]{spreadview::compose_spreadsheet()}}. The variables to tabulate are picked
automatically with \code{\link[spreadview:get_categorical_vars]{spreadview::get_categorical_vars()}} - every factor column
(plus character columns when \code{include_char} is on), minus any the user chooses
to \code{exclude}. Optional grouping variables and a survey weight are forwarded to
\code{compose_spreadsheet()}.

The block's output panel previews the (pass-through) data set that will be
exported as a table.
}
\details{
The block passes its input data through unchanged, so it can sit anywhere in a
pipeline; its output panel previews the data being exported and the \emph{Download}
button writes the \code{.xlsx} file.

Excel export requires the \pkg{spreadview} package (GitHub only). If it is not
installed the download reports an error explaining how to install it.
}
\examples{
new_stem_spreadsheet_export_block()

}
