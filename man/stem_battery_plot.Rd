% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_visualize_battery_block.R
\name{stem_battery_plot}
\alias{stem_battery_plot}
\title{Plot a battery of same-scale items, with level validation}
\usage{
stem_battery_plot(
  data,
  items,
  weight = NULL,
  order_by = NULL,
  item_label = TRUE,
  palette = "div1",
  direction = 1,
  labels = TRUE,
  label_accuracy = 1,
  label_hide = 0.05,
  reverse_levels = FALSE
)
}
\arguments{
\item{data}{Data frame holding the item (and weight) columns.}

\item{items}{Character vector of column names to plot as the battery.}

\item{weight}{Optional name of a numeric survey-weight column (\code{NULL}/\code{""} for
unweighted).}

\item{order_by}{Optional character vector of response categories used to order
the items (see \code{\link[stemtools:stem_battery]{stemtools::stem_battery()}}).}

\item{item_label, palette, direction, labels, label_accuracy, label_hide}{Passed
straight through to \code{\link[stemtools:stem_battery]{stemtools::stem_battery()}}.}

\item{reverse_levels}{If \code{TRUE}, reverse each item's response categories
(factor levels) before plotting, flipping the orientation of the response
scale along the bars. Defaults to \code{FALSE}.}
}
\value{
A ggplot2 object from \code{\link[stemtools:stem_battery]{stemtools::stem_battery()}}.
}
\description{
Runtime helper behind \code{\link[=new_stem_visualize_battery_block]{new_stem_visualize_battery_block()}}: validates that the
chosen \code{items} are categorical and \strong{share identical response categories}
(factor levels / character values) and then draws them with
\code{\link[stemtools:stem_battery]{stemtools::stem_battery()}} - one stacked horizontal bar per item. Splitting
the check out (rather than calling \code{stem_battery()} directly from the block's
emitted expression) lets a mismatched selection - items from \emph{different}
scales that cannot sensibly share one axis - fail with an informative error
the block can show, instead of \code{stem_battery()}'s internal reshape silently
unioning the levels into a nonsense chart.
}
\examples{
if (requireNamespace("stemtools", quietly = TRUE)) {
  df <- data.frame(
    q1 = factor(c("Agree", "Disagree"), levels = c("Disagree", "Agree")),
    q2 = factor(c("Disagree", "Agree"), levels = c("Disagree", "Agree"))
  )
  stem_battery_plot(df, items = c("q1", "q2"))
}
}
