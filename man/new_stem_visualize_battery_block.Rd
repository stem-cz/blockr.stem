% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_visualize_battery_block.R
\name{new_stem_visualize_battery_block}
\alias{new_stem_visualize_battery_block}
\title{STEM Visualize battery block}
\usage{
new_stem_visualize_battery_block(
  items = character(),
  weight = character(),
  order_by = character(),
  item_label = TRUE,
  palette = "div1",
  direction = 1,
  labels = TRUE,
  label_accuracy = 1,
  label_hide = 0.05,
  reverse_levels = FALSE,
  label_size = 14,
  ink = "black",
  paper = "white",
  accent = "#35978F",
  family = "Calibri",
  font_size = NA_real_,
  padding = stem_default_padding,
  ...
)
}
\arguments{
\item{items}{Character vector of variable (column) names to plot as the
battery. When empty, the block waits for a selection.}

\item{weight}{Optional name of a numeric column of survey weights. Empty (the
default) plots unweighted proportions.}

\item{order_by}{Optional character vector of response categories used to order
the items by their combined share, e.g. \code{c("Strongly agree", "Agree")}. Its
picker is populated with the selected items' shared categories.}

\item{item_label}{If \code{TRUE} (default), label each bar with the variable's
\code{"label"} attribute instead of its name (falls back to the name if absent).}

\item{palette, direction, labels, label_accuracy}{Chart styling passed to
\code{\link[stemtools:stem_battery]{stemtools::stem_battery()}} (see \code{\link[=new_stem_chart_block]{new_stem_chart_block()}}).}

\item{label_hide}{Proportions below this threshold are left unlabelled.
Defaults to \code{0.05}.}

\item{reverse_levels}{If \code{TRUE}, reverse the items' response categories
(factor levels) to flip the orientation of the response scale in the plot.
Defaults to \code{FALSE}.}

\item{label_size}{Size in points of the inner data labels. Defaults to \code{14};
\code{NA} keeps the stemtools default.}

\item{ink, paper, accent, family}{Stem theme arguments (see
\code{\link[=new_theme_stem_block]{new_theme_stem_block()}} and \code{\link[stemtools:theme_stem]{stemtools::theme_stem()}}).}

\item{font_size}{Base text size in points for the axis/legend/title text.
\code{NA} (the default) keeps the theme's own sizing.}

\item{padding}{Padding in millimetres added to the plot margin so the extreme
axis labels are not clipped. Defaults to 8 mm.}

\item{...}{Forwarded to \code{\link[blockr.ggplot:new_ggplot_transform_block]{blockr.ggplot::new_ggplot_transform_block()}}.}
}
\value{
A ggplot transform block object of class
\code{stem_visualize_battery_block}.
}
\description{
A single blockr block that plots a \strong{battery of same-scale categorical
items} - typically a set of Likert items sharing one response scale - as a
stacked-bar chart with \code{\link[stemtools:stem_battery]{stemtools::stem_battery()}} (one horizontal bar per
item) \emph{and} applies the Stem theme, so the final, styled chart is produced in
one place. The user selects several variables from a list; only variables that
share \strong{identical response categories} (factor levels) can be plotted
together - a mismatched selection produces an informative error (see
\code{\link[=stem_battery_plot]{stem_battery_plot()}}). The block emits a plain ggplot, so it pipes cleanly
into \code{\link[=new_stem_export_block]{new_stem_export_block()}} for PNG / SVG / PowerPoint download.
}
\details{
Chart options and theme options live under separate "Advanced" disclosures,
mirroring \code{\link[=new_stem_visualize_block]{new_stem_visualize_block()}}.
}
\examples{
new_stem_visualize_battery_block(items = c("q1", "q2"), accent = "#35978F")

}
