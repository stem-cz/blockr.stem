% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_export_block.R
\name{new_stem_export_block}
\alias{new_stem_export_block}
\alias{block_ui.stem_export_block}
\alias{block_output.stem_export_block}
\title{STEM Export block}
\usage{
new_stem_export_block(
  format = c("png", "svg", "pptx"),
  width = 14,
  height = NA_real_,
  scale = 1.5,
  ...
)

\method{block_ui}{stem_export_block}(id, x, ...)

\method{block_output}{stem_export_block}(x, result, session)
}
\arguments{
\item{format}{Export format, one of \code{"png"}, \code{"svg"} or \code{"pptx"}.}

\item{width}{Export width in centimetres (default \code{14}).}

\item{height}{Export height in centimetres. \code{NA} (the default) picks a height
from the plot type (6 cm inline, 10 cm bar plot).}

\item{scale}{Multiplicative scaling factor passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}'s
\code{scale} argument for the PNG/SVG image formats (default \code{1.5}). Because the
output size is fixed, a \strong{higher} number makes the plot's text and marks
appear \strong{smaller} (and a lower number makes them larger). Ignored by the
PowerPoint chart export, which is not rasterised.}

\item{...}{Forwarded to \code{\link[blockr.ggplot:new_ggplot_transform_block]{blockr.ggplot::new_ggplot_transform_block()}}.}

\item{id, x}{Passed by blockr when rendering the block UI.}

\item{x, result, session}{Passed by blockr when rendering the block output.}
}
\value{
A ggplot transform block object of class \code{stem_export_block}.
}
\description{
A blockr block that previews an upstream ggplot and lets the user download it
as a \strong{PNG} or \strong{SVG} image, or as a \strong{native PowerPoint chart} (\code{.pptx}),
at a chosen size in centimetres. Width defaults to 14 cm; height defaults to
the plot type - 6 cm for an inline plot (\code{\link[stemtools:stem_inline]{stemtools::stem_inline()}}) and
10 cm for a bar plot (\code{\link[stemtools:stem_barplot]{stemtools::stem_barplot()}}) - and can be overridden.

The block's output panel shows a live preview of the plot \emph{exactly as it will
be exported}: it is produced by the same \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} call as the
download, so the chosen width/height (cm) set the aspect ratio and the scaling
factor changes the apparent size of the text and marks. The preview updates
automatically whenever the format, width, height or scaling inputs change.
}
\details{
SVG export uses \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}'s \code{"svg"} device, which requires the
\pkg{svglite} package; if it is not installed the SVG download reports an
error and PNG can be used instead.

PowerPoint export writes a \strong{native, editable Office chart} (not a picture)
via \code{\link[=stem_write_pptx_chart]{stem_write_pptx_chart()}} - the recipient can \emph{Edit Data} in Excel and
restyle it by hand. It requires the \pkg{mschart} and \pkg{officer} packages
and only works for the STEM bar / inline plots (see \code{\link[=stem_write_pptx_chart]{stem_write_pptx_chart()}}).
}
\examples{
new_stem_export_block(format = "png", width = 14)

}
