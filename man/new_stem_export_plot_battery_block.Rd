% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_export_plot_battery_block.R
\name{new_stem_export_plot_battery_block}
\alias{new_stem_export_plot_battery_block}
\alias{block_ui.stem_export_plot_battery_block}
\alias{block_output.stem_export_plot_battery_block}
\title{STEM Export (chart or battery) block}
\usage{
new_stem_export_plot_battery_block(
  target = c("plot", "battery"),
  format = c("png", "svg", "pptx"),
  width = 14,
  height = NA_real_,
  scale = 1.5,
  ...
)

\method{block_ui}{stem_export_plot_battery_block}(id, x, ...)

\method{block_output}{stem_export_plot_battery_block}(x, result, session)
}
\arguments{
\item{target}{Which upstream input to export by default, \code{"plot"} (the STEM
Visualize input) or \code{"battery"} (the STEM Visualize battery input).}

\item{format}{Export format, one of \code{"png"}, \code{"svg"} or \code{"pptx"}.}

\item{width}{Export width in centimetres (default \code{14}).}

\item{height}{Export height in centimetres. \code{NA} (the default) picks a height
from the plot type (6 cm inline, 10 cm bar plot).}

\item{scale}{Multiplicative scaling factor passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}'s
\code{scale} argument for the PNG/SVG image formats (default \code{1.5}). Because the
output size is fixed, a \strong{higher} number makes the plot's text and marks
appear \strong{smaller} (and a lower number makes them larger). Ignored by the
PowerPoint chart export, which is not rasterised.}

\item{...}{Forwarded to \code{\link[blockr.ggplot:new_ggplot_transform_block]{blockr.ggplot::new_ggplot_transform_block()}}.}

\item{id}{Passed by blockr when rendering the block UI.}

\item{x, result, session}{Passed by blockr when rendering the block UI/output.}
}
\value{
A ggplot transform block object of class
\code{stem_export_plot_battery_block}.
}
\description{
A two-input variant of \code{\link[=new_stem_export_block]{new_stem_export_block()}}: it accepts a plot from a
\strong{STEM Visualize} block \emph{and} a plot from a \strong{STEM Visualize battery} block
and lets the user pick which one to export with a toggle. Everything else -
the PNG / SVG / native PowerPoint export, the size / scaling controls and the
live preview - works exactly as in \code{\link[=new_stem_export_block]{new_stem_export_block()}} (the two blocks
share the same export helpers and preview renderer).

The block's output panel shows the same live preview as \code{\link[=new_stem_export_block]{new_stem_export_block()}}
\itemize{
\item the selected plot rendered by the very \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} call used for the
download - via the shared preview renderer.
}
}
\details{
Both inputs are \strong{optional}, so the block works with just one wired up: the
toggle chooses the export target when both are connected, but falls back to
whichever input \emph{is} connected when the chosen one is missing. When neither is
connected the block waits (blank preview) rather than erroring.
}
\examples{
new_stem_export_plot_battery_block(target = "battery", format = "png")

}
